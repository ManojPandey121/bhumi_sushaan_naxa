import 'package:flutter/material.dart';

class PriorityProjects extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(title: new Text("मुख्य परियोजना"),),
      
    );
  }
}